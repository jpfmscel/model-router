{
  "name": "model-router",
  "description": "Route each task to the best Claude model tier via the haiku/sonnet/opus aliases (always the latest models), using model-pinned subagents, a decision-rubric skill, a /pick-model command, and a per-prompt hook that triggers the routing skill automatically.",
  "version": "0.3.0",
  "author": { "name": "jpfmscel" },
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/pick-model-trigger.sh\"",
            "timeout": 5,
            "statusMessage": "Routing…"
          }
        ]
      }
    ]
  }
}
